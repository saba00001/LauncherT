<Window x:Class="HRP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="HRP Launcher" 
        Height="650" 
        Width="950"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Loaded="MainWindow_Loaded">

    <Window.Resources>
        <!-- Enhanced Button Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource MediumBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LightBackgroundBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="270" ShadowDepth="3" Opacity="0.6" BlurRadius="12"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" BasedOn="{StaticResource ModernButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="270" ShadowDepth="3" Opacity="0.5" BlurRadius="15"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF00BCF2" Offset="0"/>
                                            <GradientStop Color="#FF40E0D0" Offset="0.5"/>
                                            <GradientStop Color="#FF0078D4" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource AccentBrush}" Direction="270" ShadowDepth="4" Opacity="0.8" BlurRadius="20"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF333333"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF333333"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource MediumBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="SelectionBrush" Value="#4400BCF2"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="False" 
                                        HorizontalScrollBarVisibility="Hidden" 
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource SecondaryBrush}" Direction="270" ShadowDepth="2" Opacity="0.4" BlurRadius="12"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="270" ShadowDepth="3" Opacity="0.6" BlurRadius="15"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced ProgressBar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <Rectangle x:Name="PART_Track" Fill="{TemplateBinding Background}"/>
                                <Rectangle x:Name="PART_Indicator" 
                                         Fill="{TemplateBinding Foreground}" 
                                         HorizontalAlignment="Left"
                                         RadiusX="6" RadiusY="6">
                                    <Rectangle.Effect>
                                        <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="0" ShadowDepth="0" Opacity="0.8" BlurRadius="10"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Title Bar Button Style -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LightBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource MediumBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButton" BasedOn="{StaticResource TitleBarButton}" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFFF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFCC3333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MediumBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" Opacity="0.4" BlurRadius="16"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- System Tray Icon -->
    <tb:TaskbarIcon x:Name="SystemTrayIcon"
                    ToolTipText="HRP Launcher"
                    DoubleClickCommand="{Binding ShowWindowCommand}"
                    Visibility="Collapsed">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="გახსნა" Click="ShowWindow_Click"/>
                <Separator/>
                <MenuItem Header="გასვლა" Click="ExitApplication_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>

    <!-- Main Window Border -->
    <Border Background="{StaticResource BackgroundGradient}" 
            CornerRadius="16" 
            BorderBrush="{StaticResource PrimaryBrush}" 
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="315" ShadowDepth="12" Opacity="0.6" BlurRadius="24"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource MediumBackgroundBrush}" 
                    CornerRadius="16,16,0,0" 
                    BorderBrush="{StaticResource LightBackgroundBrush}" 
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center" 
                                Margin="25,0,0,0">
                        <Ellipse Width="12" Height="12" 
                                 Fill="{StaticResource PrimaryGradient}" 
                                 Margin="0,0,12,0">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="0" ShadowDepth="0" Opacity="0.8" BlurRadius="8"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="HRP Launcher v2.0" 
                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                   FontSize="16" 
                                   FontWeight="Medium"/>
                        <TextBlock Text="• მზადაა გამოყენებისთვის" 
                                   Foreground="{StaticResource SuccessBrush}" 
                                   FontSize="12" 
                                   FontWeight="Light"
                                   Margin="15,0,0,0"
                                   x:Name="TitleStatusText"/>
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal"
                                Margin="0,0,10,0">
                        <Button x:Name="SettingsButton" 
                                Content="⚙" 
                                Style="{StaticResource TitleBarButton}"
                                Click="SettingsButton_Click"
                                ToolTip="პარამეტრები"/>
                        <Button x:Name="MinimizeButton" 
                                Content="─" 
                                Style="{StaticResource TitleBarButton}"
                                Click="MinimizeButton_Click"
                                ToolTip="ჩახურვა"/>
                        <Button x:Name="CloseButton" 
                                Content="✕" 
                                Style="{StaticResource CloseButton}"
                                Click="CloseButton_Click"
                                ToolTip="დახურვა"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Main Content -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Logo/Header Section -->
                    <Border Grid.Row="0" Style="{StaticResource ModernCard}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="HRP" 
                                       FontSize="54" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryGradient}"
                                       HorizontalAlignment="Center"
                                       RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform/>
                                </TextBlock.RenderTransform>
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="270" ShadowDepth="2" Opacity="0.8" BlurRadius="15"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text="High Quality Roleplay Server" 
                                       FontSize="18" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,8,0,0"/>
                            <Rectangle Height="3" 
                                       Width="300" 
                                       Fill="{StaticResource PrimaryGradient}" 
                                       Margin="0,15,0,0"
                                       RadiusX="2" RadiusY="2">
                                <Rectangle.Effect>
                                    <DropShadowEffect Color="{StaticResource PrimaryBrush}" Direction="0" ShadowDepth="0" Opacity="0.8" BlurRadius="8"/>
                                </Rectangle.Effect>
                            </Rectangle>
                        </StackPanel>
                    </Border>

                    <!-- Player Name Input -->
                    <Border Grid.Row="2" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Text="🎮 მოთამაშის სახელი" 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontSize="16" 
                                       FontWeight="Medium"
                                       Margin="0,0,0,12"/>
                            <TextBox x:Name="PlayerNameTextBox" 
                                     Height="45" 
                                     Style="{StaticResource ModernTextBox}"
                                     MaxLength="25"
                                     Text="Player"
                                     FontSize="16"
                                     TextChanged="PlayerNameTextBox_TextChanged"/>
                            <TextBlock Text="შეიყვანეთ თქვენი სათამაშო სახელი (მაქსიმუმ 25 სიმბოლო)" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       FontSize="11" 
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Server Status -->
                    <Border Grid.Row="4" Style="{StaticResource ModernCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="📊 სერვერის სტატუსი" 
                                           Foreground="{StaticResource TextPrimaryBrush}" 
                                           FontSize="16" 
                                           FontWeight="Medium"
                                           Margin="0,0,0,8"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4">
                                    <Ellipse Width="10" Height="10" x:Name="ServerStatusIndicator" Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="ServerStatusText" Text="ონლაინია" Foreground="{StaticResource SuccessBrush}" FontWeight="Medium"/>
                                </StackPanel>
                                <TextBlock x:Name="PlayersOnlineText" Text="მოთამაშეები: 245/500" 
                                           Foreground="{StaticResource TextSecondaryBrush}" 
                                           FontSize="12" 
                                           Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="📡 კავშირი" 
                                           Foreground="{StaticResource TextPrimaryBrush}" 
                                           FontSize="16" 
                                           FontWeight="Medium"
                                           Margin="0,0,0,8"/>
                                <TextBlock x:Name="PingText" Text="Ping: 25ms" 
                                           Foreground="{StaticResource SuccessBrush}" 
                                           FontWeight="Medium"
                                           Margin="0,4"/>
                                <TextBlock Text="IP: 91.134.166.77:33333" 
                                           Foreground="{StaticResource TextSecondaryBrush}" 
                                           FontSize="12" 
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Progress Section -->
                    <Border Grid.Row="6" Style="{StaticResource ModernCard}" x:Name="ProgressCard" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="ProgressLabel" 
                                       Text="ფაილების ჩამოწერა..." 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       HorizontalAlignment="Center" 
                                       Margin="0,0,0,12"/>
                            <ProgressBar x:Name="DownloadProgressBar" 
                                         Height="20"
                                         Style="{StaticResource ModernProgressBar}"/>
                            <TextBlock x:Name="ProgressPercentText" 
                                       Text="0%" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       FontSize="12" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <Grid Grid.Row="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" 
                                x:Name="CheckUpdateButton" 
                                Content="🔄 განახლების შემოწმება" 
                                Style="{StaticResource ModernButton}" 
                                Height="50" 
                                FontSize="14"
                                Click="CheckUpdateButton_Click"/>

                        <Button Grid.Column="2" 
                                x:Name="LaunchGameButton" 
                                Content="▶ თამაშის დაწყება" 
                                Style="{StaticResource PrimaryButton}" 
                                Height="55" 
                                FontSize="16"
                                Click="LaunchGameButton_Click"/>
                    </Grid>
                </Grid>

                <!-- Right Panel - News/Info -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="15"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="15"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Version Info -->
                    <Border Grid.Row="0" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Text="📋 ვერსიის ინფორმაცია" 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Text="მიმდინარე:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Width="70"/>
                                <TextBlock x:Name="CurrentVersionLabel" Text="v2.0.1" Foreground="{StaticResource SuccessBrush}" FontSize="12" FontWeight="Medium"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Text="უახლესი:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Width="70"/>
                                <TextBlock x:Name="LatestVersionLabel" Text="v2.0.1" Foreground="{StaticResource SuccessBrush}" FontSize="12" FontWeight="Medium"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Ellipse Width="8" Height="8" x:Name="UpdateStatusIndicator" Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                                <TextBlock x:Name="StatusLabel" Text="განახლებულია!" Foreground="{StaticResource SuccessBrush}" FontSize="12" FontWeight="Medium"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- News/Updates Section -->
                    <Border Grid.Row="2" Style="{StaticResource ModernCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="📰 სიახლეები" 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Margin="0,0,0,10"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel x:Name="NewsPanel">
                                    <Border Background="{StaticResource DarkBackgroundBrush}" 
                                            CornerRadius="6" 
                                            Padding="12" 
                                            Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="ახალი განახლება v2.0!" 
                                                       Foreground="{StaticResource AccentBrush}" 
                                                       FontSize="12" 
                                                       FontWeight="Bold"/>
                                            <TextBlock Text="დამატებულია Discord Rich Presence მხარდაჭერა და გაუმჯობესებული UI." 
                                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                                       FontSize="11" 
                                                       TextWrapping="Wrap"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Background="{StaticResource DarkBackgroundBrush}" 
                                            CornerRadius="6" 
                                            Padding="12" 
                                            Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="სერვერის ოპტიმიზაცია" 
                                                       Foreground="{StaticResource SecondaryBrush}" 
                                                       FontSize="12" 
                                                       FontWeight="Bold"/>
                                            <TextBlock Text="გაუმჯობესებულია სერვერის მუშაობა და დამატებულია ახალი ფუნქციები." 
                                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                                       FontSize="11" 
                                                       TextWrapping="Wrap"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Discord & Social -->
                    <Border Grid.Row="4" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Text="🔗 სოციალური ქსელები" 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Margin="0,0,0,10"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Button Grid.Column="0" 
                                        Content="💬 Discord" 
                                        Style="{StaticResource ModernButton}"
                                        Height="35"
                                        FontSize="11"
                                        Margin="0,0,5,0"
                                        Click="DiscordButton_Click"/>
                                
                                <Button Grid.Column="1" 
                                        Content="🌐 ვებსაიტი" 
                                        Style="{StaticResource ModernButton}"
                                        Height="35"
                                        FontSize="11"
                                        Margin="5,0,0,0"
                                        Click="WebsiteButton_Click"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal" 
                                        HorizontalAlignment="Center" 
                                        Margin="0,10,0,0">
                                <Ellipse Width="8" Height="8" 
                                         x:Name="DiscordStatusIndicator" 
                                         Fill="{StaticResource SuccessBrush}" 
                                         Margin="0,0,6,0"/>
                                <TextBlock x:Name="DiscordStatusText" 
                                           Text="Discord დაკავშირებულია" 
                                           Foreground="{StaticResource SuccessBrush}" 
                                           FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>